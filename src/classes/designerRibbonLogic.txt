btnDelGeo: Delete the geo. Just create a LLGeo object and apply its .Clear method. Geoboject should be created with the Geo worksheet
btnClear: Clear entries. Just create a DesignerEntry object using current worksheet and apply the .Clear method

chkAlert, chkInstruct use HiddenNames at the designer workbook level (with same names) and toogle their values
to Yes if the checkbox is checked/unchecked.

Add same code logic for clickDevInitialize for setup but with a designer preparation class instead of setup preparation

when the language dropdown changes, apply a .Translate method to the designerEntry; and invalidate the ribbon
(the logic on RibbonDev is the same as the one of the setup, you can get the ribon from there)

We will work progressively on the CustomGroupMulti part later on